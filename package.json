{
  "name": "exodus40lite",
  "version": "2.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for the Exodus rule-of-life tracker",
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "sync": "npm run build:www && cap sync android",
    "build:android": "npm run sync && (cd android && ./gradlew assembleRelease bundleRelease)"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/local-notifications": "^6.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
